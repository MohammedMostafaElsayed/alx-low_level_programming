#include "main.h"

/**
 * rev_string - revesing
 * @s: input
 *
 * Return: no 
 */
void rev_string(char *s)
{
	int i;
	int j;
	int x;
	
	for (i = 0; *(s + i) != '\0'; i++)
	{
	}
	int z[i];
	for (x = i-1; x >= 0; x--)
	{
		for(j = 0; j < i - 1; j++)
		{
			z[j] = s[x];
		}
	}
	_putchar('\n');
}
