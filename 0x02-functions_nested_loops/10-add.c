#include "main.h"
#include <stdio.h>

/**
 * add - this fun add two integer
 * @m: is first int
 * @h: is secend int
 *
 * Return: this retirn the sum
 */
int add(int m, int h)
{
	int sum;

	sum = m + h;
	return (sum);
}

